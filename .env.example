# PostgreSQL
POSTGRES_USER=dash
POSTGRES_PASSWORD=dash
POSTGRES_DB=dash_financeiro
DB_PORT=5432

# Backend
BACKEND_PORT=8000
BRAPI_TOKEN=
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# JWT (REQUIRED — generate a strong random secret)
JWT_SECRET=
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Google OAuth (optional)
GOOGLE_CLIENT_ID=

# Admin seed (REQUIRED — set your admin credentials)
ADMIN_EMAIL=
ADMIN_PASSWORD=

# SMTP (optional — falls back to console)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
EMAIL_VERIFY_URL=http://localhost:5173

# Frontend
FRONTEND_PORT=3000
